version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: travel
      POSTGRES_USER: travel_user
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
  redis:
    image: redis:7
    ports:
      - "6379:6379"
